/**
 * Test simple para verificar que el archivo no está vacío
 */

describe('Tests simples de imports', () => {
    test('archivo de test válido', () => {
        expect(true).toBe(true);
    });
});
