/**
 * Stub de @vue/devtools-api para tests e2e.
 * pinia y vue-router importan setupDevtoolsPlugin desde aquí.
 * En un entorno de prueba no necesitamos devtools reales.
 */
export function setupDevtoolsPlugin(_options, _setupFn) {
    // no-op en tests
}
